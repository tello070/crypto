// Updating the import from @/components/ui/use-toast to @/hooks/use-toast
import { useToast } from "@/hooks/use-toast";

// Rest of the file remains exactly the same as in the diff, just with the updated import path